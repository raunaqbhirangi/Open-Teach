defaults:
  - _self_
  - network
  - camera
  - robot: onrobot_kinova
  - override hydra/hydra_logging: disabled  
  - override hydra/job_logging: disabled  

 
use_sensor: false

hydra:  
  output_subdir: null  
  run:  
    dir: .