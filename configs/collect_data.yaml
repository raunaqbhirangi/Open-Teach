defaults:
  - _self_
  - camera
  - network
  - reskin
  - robot: xarm_stick
  - override hydra/hydra_logging: disabled  
  - override hydra/job_logging: disabled 

storage_path: extracted_data/0606_rand
demo_num: 0
sim_env: false
use_sensor: false
host: ${host_address}



hydra:  
  output_subdir: null  
  run:  
    dir: .
